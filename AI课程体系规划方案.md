# AI 技能升级课程体系 · 规划方案

> 目的：在《整理稿》基础上，把课程体系落成**可直接驱动前端展示与后续生产的结构化方案**，含数据模型、模块定义、能力映射、MVP 优先级与路线图。
> 前端交互网页（`ai-curriculum.html`）即基于本方案的 JSON 数据模型生成。

---

## 一、产品定位

- **产品形态**：分人群、场景化、成果交付的 AI 技能课程产品矩阵（非单一 Prompt 课）。
- **核心价值**：把公开 AI 知识重组为「分人群学习路径 + 场景化任务 + 可复制模板 + 可展示成果 + 可交付课程包」。
- **商业模式优先级**：普通社会人员（P0）> 产品经理（P0）> 中学生+大学生（P1）> 儿童（P1）。

---

## 二、数据模型（前端 + 后续生产共用）

### 2.1 Persona（人群线）对象
```
{
  id, name(课程名), audience(人群), ageRange, color,
  goal(核心目标), focus(学习重点[]), outcome(最终成果),
  modules[ {no, title, task(任务产出)} ],
  deliverables(生产交付物[]), validation(验收红线[]),
  capabilityWeights(五层能力权重 -> 0~5)
}
```

### 2.2 Capability（能力层）对象
```
{ id, name, meaning, personas[] }
```

### 2.3 课程匹配测试模型
- 输入：4 道单选题（身份 / 目标 / 当前水平 / 痛点场景）。
- 输出：映射到 1 条 Persona 主线 + 推荐 MVP 包 + 起步任务。
- 规则：基于 audience 标签计分，最高分命中。

### 2.4 路线图对象
```
phases[ { phase, name, items[] } ]  // 三阶段
```

### 2.5 资源对象
```
resources[ { category, items[ { repo, note, audience } ] } ]
```

---

## 三、五条主线（含能力权重）

| 主线 | 人群 | 能力权重（素养/使用/协作/工作流/产品）|
|---|---|---|
| 儿童·AI 创造力启蒙营 | 6–12 岁 + 家长 | 5/2/1/0/0 |
| 中学生·AI 学习力训练营 | 初中~高中 | 5/4/2/0/0 |
| 大学生·AI 项目与职业能力营 | 大学生/研究生/应届 | 4/4/4/3/2 |
| 职场人·AI 工作提效实战营 | 行政/销售/客服/运营/个体户等 | 4/5/4/5/1 |
| 产品经理·AI 产品经理进阶营 | 产品经理/创业者 | 4/4/4/5/5 |

> 权重用于前端「能力雷达」可视化与课程推荐。

---

## 四、各主线模块（8 / 8 / 8–10 / 8 / 10 节）

详细模块见《整理稿》第四节，前端以 `modules[]` 数组承载；此处仅列节数与定位：

- **儿童 8 节**：AI 是什么 → 提问 → 讲故事 → 画角色 → 认世界 → 辨真假 → 未来发明 → 创作展示。
- **中学生 8 节**：助手入门 → 好问题 → 难题拆解 → 英写训练 → 复习 → 真假判断 → 演讲 → 助手设计。
- **大学生 8–10 节**：定位 → 调研 → 论文 → 数据 → 简历面试 → 项目设计 → 原型 → 作品集（+可选扩展）。
- **职场人 8 节 + 岗位包**：工具入门 → 写作 → 会议 → 销售客服 → 内容 → 数据 → 管理招聘 → 私人工作流。
- **PM 10 节**：基础 → 差异 → 交互 → RAG → Agent → 多模态 → 评估 → 约束 → 风险合规 → 商业化。

---

## 五、MVP 与路线图

### MVP（先落地）
- **MVP A｜职场人工作提效营**：8 教案 + 8 PPT + 50 场景 Prompt + 6 岗位包 + 1 工作流模板 + 1 公开课 + 1 落地页。
- **MVP B｜PM 产品经理进阶营**：10 教案 + 10 PPT + PRD 模板 + Agent 模板 + 评估表 + 5 案例 + 1 公开课 + 1 落地页。

### 三阶段路线图
1. **MVP**：工作提效营 + PM 营 + 双落地页 + 双试听课 + 双模板包。
2. **扩展学生儿童**：学习力营 + 创造力营 + 家长说明书 + 校/机构合作版。
3. **平台化**：能力测评 + 路径推荐 + 任务打卡 + 作品集展示 + 企业/学校团培版。

---

## 六、前端交互网页功能清单

| 模块 | 功能 | 数据来源 |
|---|---|---|
| 顶部导航 | 概览 / 选课程 / 能力模型 / 路线图 / 匹配测试 / 资源 | 静态 |
| 身份选择器 | 5 张卡片，点击展开该线详情（目标/模块/交付物/验收/能力雷达）| personas[] |
| 能力模型 | 五层能力表 + 各线权重对比条形 | capabilities[] + capabilityWeights |
| 路线图 | 三阶段时间线 | phases[] |
| 课程匹配测试 | 4 题 → 命中主线 + 推荐 MVP + 起步建议 | 匹配规则 |
| 资源库 | GitHub 资源按方向折叠 | resources[] |
| 风险边界 | 全局红线提示 | 静态 |

**技术约束**：单文件 HTML，内联 CSS + 原生 JS，零外部依赖（可离线打开、可直发）。深色/浅色自适应，移动端可用。

---

## 七、后续生产衔接

前端 JSON 数据模型可直接作为生产 agent 的「课程结构骨架」：
- 每条 `modules[]` = 一节待生成的详细教案（教学目标/案例/Prompt/检查清单/作业/评分）。
- `deliverables[]` = 该线生产任务清单。
- `capabilityWeights` = 内容深浅与案例密度调节参数。

> 规划方案定稿后，下一步即执行 MVP A 任务 2（职场人 8 节完整课程包）。
